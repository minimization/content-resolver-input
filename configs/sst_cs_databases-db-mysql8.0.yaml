document: feedback-pipeline-workload
version: 1
data:
  name: MySQL 8.0 client and server
  description: Client tools and a server daemon for MySQL 8.0 from Oracle
  maintainer: sst_cs_databases
  packages:
    - mysql8.0
    - mysql8.0-server
    - mysql8.0-test
    - mysql8.0-devel
    - mysql-selinux # Because RPM mysql8.0-server Requires: (mysql-selinux if selinux-policy-targeted)
    - mecab-ipadic
    - mecab-ipadic-EUCJP
  labels:
    - eln
