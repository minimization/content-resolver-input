---
document: feedback-pipeline-workload
version: 1
data:
  name: NetworkManager dispatcher routing rules
  description: >
    NetworkManager dispatcher file for advanced routing rules

  maintainer: sst_network_management
  packages:
    # Dispatcher routing rules 
    - NetworkManager-dispatcher-routing-rules
  labels:
    - c9s
